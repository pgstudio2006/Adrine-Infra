# @adrine/api-contracts

Holds **OpenAPI** source files, **JSON Schema** event definitions, and generated SDK inputs. Phase 0 seeds the package boundary only; specs are generated from running services in later CI stages.
