import { Module } from '@nestjs/common';

/**
 * Operational governance hooks in domain-api.
 * Authoritative policy CRUD lives in kernel-api /governance/*.
 */
@Module({})
export class GovernanceModule {}
